# 照片标定批量入库（纯人工版）任务计划

## 本阶段范围

- 全页人工批量录入工作流；不显示仓库 3D 场景。
- 整面货架照片的人工层位标定、商品框选、SKU 选择、审核和批量导入。
- 本阶段不接入检测模型、VLM、自动标定或自动 SKU 识别。

## 已确认的数据与坐标约定

- 商品实例的位置是其几何中心点的 `map` 世界坐标。
- `y_cm` 是商品中心沿货架局部 `+Y` 的位置。
- `width_cm` 是商品沿局部 `Y` 的宽度；`height_cm` 是商品沿局部 `Z` 的高度。
- 商品中心高度由 `层板表面高度 + height_cm / 2` 计算，不再保存 `z_offset_cm`。
- 当前不记录真实 `depth_cm`；所有商品在局部 `X` 方向按统一 `3 cm` 厚度显示，中心沿货架面的法线放置在 `shelf_depth_normal` 的中间位置。
- 新实例 ID 格式：`{shelf_id}-{face}-{level}-{y_cm}`。
- 图片目录按实例 ID 保存：`data/item_images/{slot_id}/0.png`；数据库只保存该相对目录。
- 选择货架面后由程序固定照片与局部 `Y` 的关系：
  - `-X` 侧：照片右侧为 `y=0`，向左为 `+Y`。
  - `+X` 侧：照片左侧为 `y=0`，向右为 `+Y`。

## 任务清单

- [x] 1. 核对并复用现有 `shelf_type` 的层板表面高度、层间可用高度和长度，作为人工标定的真实尺寸来源。
- [x] 2. 设计并执行数据库迁移：删除 `z_offset_cm`，新增可空 `width_cm`、`height_cm`、`image_dir`，并将现有数据迁移为 `height_cm = 2 * z_offset_cm`。
- [x] 3. 调整 `slot_id` 解析、唯一性约束、坐标计算和相关 API，使用四段 ID 与 `height_cm / 2` 计算商品中心；保留统一 `3 cm` 深度的显示/位置规则。
- [x] 4. 同步更新初始化数据、Web 类型、场景渲染、编辑表单、文档和测试，移除运行时全部 `z_offset_cm` 路径。
- [x] 5. 增加批量导入服务端接口：校验 SKU、层位、标定结果、ID 冲突和图片数据；在一个 SQLite 事务中写入新 SKU 与商品实例。
- [x] 6. 实现图片暂存、裁剪、正式目录落盘和失败清理机制；数据库仅写入受控的相对图片目录。
- [x] 7. 在全局导航增加“人工批量录入”入口，并实现独立全页工作流的路由与返回行为。
- [x] 8. 实现第一步：选择货架、`-X/+X` 面、上传整面照片，并显示照片原点与 `+Y/+Z` 方向提示。
- [x] 9. 实现第二步：四点层位标定工具；每个标定区域绑定层号，并按已知层位尺寸建立像素到局部 `(Y, Z)` 的映射。
- [x] 10. 实现第三步：商品矩形框选、按四角完整落入单一层位的自动归属、SKU 搜索选择与仅填名称的快速新建 SKU。
- [x] 11. 从商品框中心和标定映射计算 `y_cm`、`width_cm`、`height_cm`；框选时生成 `0.png`，审核预览和导入复用同一图片数据。
- [x] 12. 实现固定审核表：显示裁剪预览、实例 ID、SKU、货架面、层号、尺寸和计算坐标，支持逐行编辑、删除与导入前冲突提示。
- [x] 13. 实现确认导入、导入结果反馈和返回仓库页后的数据刷新；导入失败时不写入任何商品实例记录。
- [x] 14. 为迁移、坐标投影、ID 冲突、批量事务、图片路径和主要人工操作流程补充自动化验证。
- [ ] 15. 在桌面与移动视口检查标定画布、审核表和导入状态，修正影响标注精度或可操作性的界面问题。

## 后续 AI 辅助（不在本阶段实施）

- [ ] 用检测模型预填商品框。
- [ ] 用视觉语言模型给出 SKU 候选与新 SKU 建议。
- [ ] 用视觉模型建议层位四点标定，但必须保留人工修正与审核。
